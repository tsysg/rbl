local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer

-- Xóa GUI cũ nếu đã tồn tại
if CoreGui:FindFirstChild("FlyGuiNgocTien") then
	CoreGui:FindFirstChild("FlyGuiNgocTien"):Destroy()
end

-- Khởi tạo ScreenGui
local main = Instance.new("ScreenGui")
main.Name = "FlyGuiNgocTien"
main.Parent = CoreGui
main.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
main.ResetOnSpawn = false

-- Nút Tắt/Mở Menu chính (Phong cách Maru Hub)
local ToggleButton = Instance.new("TextButton")
local ToggleCorner = Instance.new("UICorner")

ToggleButton.Name = "ToggleButton"
ToggleButton.Parent = main
ToggleButton.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
ToggleButton.Position = UDim2.new(0, 15, 0.4, 0)
ToggleButton.Size = UDim2.new(0, 45, 0, 45)
ToggleButton.Font = Enum.Font.Garamond
ToggleButton.Text = "FLY"
ToggleButton.TextColor3 = Color3.fromRGB(0, 255, 150)
ToggleButton.TextSize = 16.000
ToggleButton.Active = true
ToggleButton.Draggable = true

ToggleCorner.CornerRadius = UDim.new(1, 0)
ToggleCorner.Parent = ToggleButton

-- Frame Menu Chính (Giao diện tối Maru Style)
local MainFrame = Instance.new("Frame")
local FrameCorner = Instance.new("UICorner")
local Title = Instance.new("TextLabel")
local FlyToggleBtn = Instance.new("TextButton")
local FlyBtnCorner = Instance.new("UICorner")
local CloseBtn = Instance.new("TextButton")

MainFrame.Name = "MainFrame"
MainFrame.Parent = main
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
MainFrame.Position = UDim2.new(0.35, 0, 0.35, 0)
MainFrame.Size = UDim2.new(0, 220, 0, 100)
MainFrame.Active = true
MainFrame.Draggable = true

FrameCorner.CornerRadius = UDim.new(0, 8)
FrameCorner.Parent = MainFrame

Title.Name = "Title"
Title.Parent = MainFrame
Title.BackgroundTransparency = 1
Title.Position = UDim2.new(0, 10, 0, 5)
Title.Size = UDim2.new(0, 170, 0, 25)
Title.Font = Enum.Font.GothamBold
Title.Text = "Fly Gui Ngoc Tien"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 14.000
Title.TextXAlignment = Enum.TextXAlignment.Left

CloseBtn.Name = "CloseBtn"
CloseBtn.Parent = MainFrame
CloseBtn.BackgroundTransparency = 1
CloseBtn.Position = UDim2.new(1, -25, 0, 5)
CloseBtn.Size = UDim2.new(0, 20, 0, 20)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 60, 60)
CloseBtn.TextSize = 14.000

FlyToggleBtn.Name = "FlyToggleBtn"
FlyToggleBtn.Parent = MainFrame
FlyToggleBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
FlyToggleBtn.Position = UDim2.new(0.08, 0, 0.45, 0)
FlyToggleBtn.Size = UDim2.new(0.84, 0, 0, 35)
FlyToggleBtn.Font = Enum.Font.GothamBold
FlyToggleBtn.Text = "Bật Fly"
FlyToggleBtn.TextColor3 = Color3.fromRGB(0, 255, 150)
FlyToggleBtn.TextSize = 14.000

FlyBtnCorner.CornerRadius = UDim.new(0, 6)
FlyBtnCorner.Parent = FlyToggleBtn

-- Bảng Điều Khiển Fly (Ẩn lúc ban đầu)
local FlyControlFrame = Instance.new("Frame")
local ControlCorner = Instance.new("UICorner")
local UpBtn = Instance.new("TextButton")
local DownBtn = Instance.new("TextButton")
local PlusBtn = Instance.new("TextButton")
local MinusBtn = Instance.new("TextButton")
local SpeedLabel = Instance.new("TextLabel")

FlyControlFrame.Name = "FlyControlFrame"
FlyControlFrame.Parent = main
FlyControlFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
FlyControlFrame.Position = UDim2.new(0.35, 0, 0.52, 0)
FlyControlFrame.Size = UDim2.new(0, 220, 0, 65)
FlyControlFrame.Visible = false
FlyControlFrame.Active = true
FlyControlFrame.Draggable = true

ControlCorner.CornerRadius = UDim.new(0, 8)
ControlCorner.Parent = FlyControlFrame

local function createControlBtn(name, text, pos, color)
	local btn = Instance.new("TextButton")
	local corner = Instance.new("UICorner")
	btn.Name = name
	btn.Parent = FlyControlFrame
	btn.BackgroundColor3 = color or Color3.fromRGB(40, 40, 40)
	btn.Position = pos
	btn.Size = UDim2.new(0, 45, 0, 25)
	btn.Font = Enum.Font.GothamBold
	btn.Text = text
	btn.TextColor3 = Color3.fromRGB(255, 255, 255)
	btn.TextSize = 12.000
	corner.CornerRadius = UDim.new(0, 4)
	corner.Parent = btn
	return btn
end

UpBtn = createControlBtn("UpBtn", "LÊN", UDim2.new(0.05, 0, 0.15, 0), Color3.fromRGB(0, 150, 255))
DownBtn = createControlBtn("DownBtn", "XUỐNG", UDim2.new(0.05, 0, 0.55, 0), Color3.fromRGB(0, 150, 255))
PlusBtn = createControlBtn("PlusBtn", "+", UDim2.new(0.75, 0, 0.15, 0), Color3.fromRGB(50, 180, 50))
MinusBtn = createControlBtn("MinusBtn", "-", UDim2.new(0.75, 0, 0.55, 0), Color3.fromRGB(200, 50, 50))

SpeedLabel.Name = "SpeedLabel"
SpeedLabel.Parent = FlyControlFrame
SpeedLabel.BackgroundTransparency = 1
SpeedLabel.Position = UDim2.new(0.3, 0, 0.25, 0)
SpeedLabel.Size = UDim2.new(0.4, 0, 0.5, 0)
SpeedLabel.Font = Enum.Font.GothamBold
SpeedLabel.Text = "Tốc độ: 1"
SpeedLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
SpeedLabel.TextSize = 12.000

-- Logic Bật/Tắt Menu
ToggleButton.MouseButton1Click:Connect(function()
	MainFrame.Visible = not MainFrame.Visible
	if not MainFrame.Visible then
		FlyControlFrame.Visible = false
	else
		if FlyToggleBtn.Text == "Tắt Fly" then
			FlyControlFrame.Visible = true
		end
	end
end)

CloseBtn.MouseButton1Click:Connect(function()
	MainFrame.Visible = false
	FlyControlFrame.Visible = false
end)

-- Logic Fly & Controls
local isFlying = false
local speeds = 1
local tpwalking = false
local tis, dis

FlyToggleBtn.MouseButton1Click:Connect(function()
	isFlying = not isFlying
	
	if isFlying then
		FlyToggleBtn.Text = "Tắt Fly"
		FlyToggleBtn.TextColor3 = Color3.fromRGB(255, 60, 60)
		FlyControlFrame.Visible = true
		
		-- Kích hoạt logic Fly
		LocalPlayer.Character.Animate.Disabled = true
		local Hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
		if Hum then
			for _, v in next, Hum:GetPlayingAnimationTracks() do v:AdjustSpeed(0) end
			Hum:ChangeState(Enum.HumanoidStateType.Swimming)
		end
		
		tpwalking = true
		for i = 1, speeds do
			task.spawn(function()
				local hb = RunService.Heartbeat
				while tpwalking and hb:Wait() and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") do
					if LocalPlayer.Character.Humanoid.MoveDirection.Magnitude > 0 then
						LocalPlayer.Character:TranslateBy(LocalPlayer.Character.Humanoid.MoveDirection)
					end
				end
			end)
		end
	else
		FlyToggleBtn.Text = "Bật Fly"
		FlyToggleBtn.TextColor3 = Color3.fromRGB(0, 255, 150)
		FlyControlFrame.Visible = false
		
		tpwalking = false
		if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
			LocalPlayer.Character.Humanoid:ChangeState(Enum.HumanoidStateType.RunningNoPhysics)
			LocalPlayer.Character.Animate.Disabled = false
		end
	end
end)

-- Nút Tăng/Giảm tốc độ
PlusBtn.MouseButton1Click:Connect(function()
	speeds = speeds + 1
	SpeedLabel.Text = "Tốc độ: " .. tostring(speeds)
end)

MinusBtn.MouseButton1Click:Connect(function()
	if speeds > 1 then
		speeds = speeds - 1
		SpeedLabel.Text = "Tốc độ: " .. tostring(speeds)
	end
end)

-- Nút Di chuyển Lên/Xuống
UpBtn.MouseButton1Down:Connect(function()
	tis = UpBtn.MouseEnter:Connect(function()
		while tis do
			task.wait()
			if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
				LocalPlayer.Character.HumanoidRootPart.CFrame = LocalPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, 1, 0)
			end
		end
	end)
end)

UpBtn.MouseLeave:Connect(function()
	if tis then tis:Disconnect() tis = nil end
end)

DownBtn.MouseButton1Down:Connect(function()
	dis = DownBtn.MouseEnter:Connect(function()
		while dis do
			task.wait()
			if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
				LocalPlayer.Character.HumanoidRootPart.CFrame = LocalPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, -1, 0)
			end
		end
	end)
end)

DownBtn.MouseLeave:Connect(function()
	if dis then dis:Disconnect() dis = nil end
end)
